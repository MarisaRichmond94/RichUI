enum Color {
  Alert = 'primary-red',
  Info = 'secondary-blue',
  Success = 'primary-green',
};

enum Horizontal {
  Bottom = 'bottom',
  Top = 'top',
};

enum Veritcal {
  Left = 'left',
  Right = 'right',
};

export {
  Color,
  Horizontal,
  Veritcal,
};
